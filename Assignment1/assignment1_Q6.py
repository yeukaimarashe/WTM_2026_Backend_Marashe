# Part 4: Sets
# 6. Remove Duplicate Values

numbers = [1, 2, 3, 4, 2, 5, 3, 6, 1]
numbers_set = set(numbers)
print(numbers_set)


# Some values were removed because sets do not allow duplicate values.
